import { Button } from 'components/atoms/Button/styles'
import styled from 'styled-components'

export const ButtonChangeThemeWrapper = styled(Button)`
  &[aria-label='Alterar o tema da pagina para escuro'] svg {
    color: #ffab00;
  }
`
