import { Story, Meta } from '@storybook/react'
import React from 'react'

import { I{{pascalCase name}}Props } from './{{camelCase name}}'

import {{pascalCase name}} from '.'

export default {
  title: '{{pascalCase name}}',
  component: {{pascalCase name}}
} as Meta

export const Desktop: Story<I{{pascalCase name}}Props> = (args) => <{{pascalCase name}} {...args} />

/*
export const Mobile: Story<I{{pascalCase name}}Props> = (args) => <{{pascalCase name}} {...args}/>

Mobile.parameters = {
 viewport: {
    defaultViewport: 'MobileMin'
  }
}
*/
